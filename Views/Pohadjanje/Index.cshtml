@model IEnumerable<StudentskaSluzba2.App_Start.Pohadjanje>

@{
    ViewBag.Title = "Index";
}

<h2>Prijavljeni ispiti</h2>

<p>
    @Html.ActionLink("Prijavi ispit", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Datum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Plan_i_program.ID_plan_i_program)
        </th>
        <th>
            Profesor
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.Prezime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Datum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Plan_i_program.ID_plan_i_program)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profesor.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.Ime)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Pohadjanje }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Pohadjanje }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Pohadjanje })
        </td>*@
    </tr>
}

</table>
