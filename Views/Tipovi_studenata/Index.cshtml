@model IEnumerable<StudentskaSluzba2.App_Start.Tipovi_studenata>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.JMBG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student_tip.Naziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.JMBG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student_tip.Naziv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Tipovi_studenata }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Tipovi_studenata }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Tipovi_studenata })
        </td>
    </tr>
}

</table>
