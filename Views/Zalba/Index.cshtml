@model IEnumerable<StudentskaSluzba2.App_Start.Zalba>

@{
    ViewBag.Title = "Žalba";
}

<h2>Žalba</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.JMBG)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.JMBG)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Zalba }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Zalba }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Zalba })
        </td>
    </tr>
}

</table>
